#include "main.h"

/**
 * _strlen - Entry point
 *
 * @s: input parameter
 *
 * Description: This program prints the length of a string
 *
 * Return: void
 *
 */

int _strlen(char *s)
{
	int i;

	for (i = 0; *(s + i) != '\0'; i++)
	{
	}

	return (i);
}
